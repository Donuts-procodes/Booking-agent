# Use this workspace to collaborate
workspace:
  id: 74ef67cc-82e1-430e-9d47-3c41b46f8bad

localResources:
  collections:
    - ../Booking Agent Service Booking API-1
  specs:
    - ../service-booking-platform/openapi.json

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ../Booking Agent Service Booking API-1: Booking Agent-service-booking-api-v1
